# Core ML
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.1.0

# Class imbalance — SMOTE (notebook 01_feature_engineering section 12)
imbalanced-learn>=0.12.0

# Gradient boosting — feature importance preview + modelling (notebook section 14)
lightgbm>=4.3.0

# Parquet I/O — 10x smaller than CSV, preserves dtypes (notebook section 13)
pyarrow>=15.0.0

# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# Config
pyyaml>=6.0.1

# Dev / testing
pytest>=8.1.0
pytest-cov>=5.0.0
httpx>=0.27.0

# Visualisation (used in notebooks)
matplotlib>=3.8.0
seaborn>=0.13.0
